## 4日目: フォーム入力を扱う

### 学ぶこと
- inputの値を`state` で管理する
- `onChange`
- `controled` `component`

### やること
- 入力欄を作る
- 入力した内容を元に表示する
- 「追加」ボタンでTODOに追加する

### ここで理解すること
- 入力値にも`state` として持つ
- フォームと`state` を同期するのがReactの基本
- 画面上の値と内部の値を一致させる

### チェックポイント
- 入力した文字列がリアルタイムで表示される
- TODO追加機能を自分で作れる